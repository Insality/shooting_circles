local panthera = require("panthera.panthera")
local animation = require("game.collections.test_panthera")
local adapter_go = require("panthera.adapters.adapter_go")

function init(self)
	print(msg.url())
	self.animation = panthera.create(animation, adapter_go, function(node)
		return go.get_id(node)
	end)
	panthera.play(self.animation, "idle")
end