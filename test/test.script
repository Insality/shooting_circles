local deftest = require("deftest.deftest")

function init(self)
	deftest.add(require("system.lifetime.test_lifetime"))
	-- {NEW_TESTS_HERE}

	local is_report = (sys.get_config_int("test.report", 0) == 1)
	deftest.run({ coverage = { enabled = is_report } })
end
